{
	"name": "@passwordless-id/mock",
	"description": "OpenID/Oauth2 mock server can be used to test libraries, implementations or perform automated integration tests.",
	"dependencies": {
		"@astrojs/cloudflare": "^14",
		"@astrojs/mdx": "^7",
		"@tsndr/cloudflare-worker-jwt": "^3.2.1",
		"astro": "^7"
	},
	"devDependencies": {
		"wrangler": "^4"
	},
	"private": true,
	"scripts": {
		"astro": "astro",
		"build": "astro build",
		"cf-typegen": "wrangler types",
		"check": "astro build && tsc && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"dev": "astro dev",
		"preview": "astro build && wrangler dev"
	},
	"type": "module"
}
